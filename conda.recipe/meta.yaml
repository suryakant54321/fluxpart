package:
  name: fluxpart
  version: "0.1.0.dev3"

source:
  fn: fluxpart-0.1.0.dev3.tar.gz
  url: https://pypi.python.org/packages/a2/06/85147d57e246fbb546ddfb4afd49dfa27712f2d20b0bd7746407994cf80b/fluxpart-0.1.0.dev3.tar.gz
  md5: a2639809d09d811d2c72d86fd3444eb7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fluxpart = fluxpart:main
    #
    # Would create an entry point called fluxpart that calls fluxpart.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy
    - pywavelets

  run:
    - python
    - numpy
    - scipy
    - pywavelets

test:
  # Python imports
  imports:
    - fluxpart

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    - pytest
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/usda-ars-ussl/fluxpart
  license: Public Domain CC0
  summary: 'Partition water vapor and carbon dioxide fluxes'
  license_family: Public-Domain

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
